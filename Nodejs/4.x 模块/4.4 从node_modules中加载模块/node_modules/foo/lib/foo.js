const user = {}
console.log(`
  id: ${module.id}                  // 当前模块ID
  filename:  ${module.filename}     // 当前模块文件文件名
  loaded:  ${module.loaded}         // 模块是否加载完毕 布尔值
  parent:  ${module.parent}         // 当前模块父对象
  children:  ${module.children}     // 当前模块子模块对象
`)
console.log(module.parent)
user.staticName = ''
user.staticFn = function () {
  console.log(`${user.staticName} form foojs`)
}
module.exports = user